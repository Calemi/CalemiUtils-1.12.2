package calemiutils.key;

import calemiutils.CUReference;
import net.minecraft.client.settings.KeyBinding;
import org.lwjgl.input.Keyboard;

public class KeyBindings {

	public static final KeyBinding openWalletButton = new KeyBinding("Open Wallet", Keyboard.KEY_G, CUReference.MOD_NAME);
}
